# git-cheat-sheet # 🛠️ My Personal Git Cheat Sheet

A quick reference guide for Git commands that I always forget.

- `git commit -m "message"` -> Saves your changes
- `git push origin main` -> Uploads to GitHub
- `git status` -> Checks what changed
